function yesButton() {
    alert("Aww! I love u so.");
}
function noHover() {
    var x = Math.random() * window.innerHeight;
    var y = Math.random() * window.innerWidth;

    document.getElementById("no").style.left = x + "px";
    document.getElementById("no").style.top = y + "px";
}